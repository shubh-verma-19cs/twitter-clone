mixin createPostForm(LoggedUser)
    .postFormContainer 
        .userImageContainer
            img(src=LoggedUser.profilePic alt="") 
        .textAreaContainer 
            textarea#postTextArea(placeholder="What's Happenning ?")
            .buttonsContainer 
                button#submitPostButton(disabled="") Post